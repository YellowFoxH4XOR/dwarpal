# rule_overrides: a valid severity reassignment loads and is surfaced by `rules`.
exec git init
exec dwarpal rules
stdout 'no-sql-concat'
stdout 'severity override: warn'

-- .dwarpal.yml --
version: 1
rule_overrides:
  "ai_patterns/no-sql-concat": "warn"
