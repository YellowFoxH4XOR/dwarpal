# rule_overrides validates its values fail-closed: a bad severity is rejected.
exec git init
! exec dwarpal rules
stderr 'invalid severity'

-- .dwarpal.yml --
version: 1
rule_overrides:
  "ai_patterns/no-broad-catch": "silent"
