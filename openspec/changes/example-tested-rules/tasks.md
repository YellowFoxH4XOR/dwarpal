## 1. Examples + check
- [x] 1.1 RegexRule gains Positives/Negatives; populate every built-in rule (assembled to avoid self-trip)
- [x] 1.2 CheckExamples verifies positives match / negatives don't / coverage present
- [x] 1.3 unit test asserts full coverage (un-regressable)

## 2. CLI
- [x] 2.1 `dwarpal rules test [--json]` subcommand; exit non-zero on failure
- [x] 2.2 rules_test.txtar

## 3. Ship
- [ ] 3.1 full suite; docs (cli.md + CHANGELOG); PR
